//  1) Includes. Antes se utilizaba el indexOf
let numbers = [1, 2, 3, 7, 8]

if (numbers.includes(7)) {
    console.log("Sí, se encuentra el valor")
} else {
    console.log('No, no se encuentra el valor')
}

// Potenciación

let base = 4
let exponent = 3
let result = base ** exponent
console.log(result)